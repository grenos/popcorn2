 <div style={{ color: 'white', fontSize: 50, margin: 30 }}>

      <div className="row-fav">
        {genres && genres.action.map((img: any) => {
          return (
            <img src={URL + img.poster} alt="" />
          )
        })}
      </div>

      <div className="row-fav">
        {genres && genres.adventure.map((img: any) => {
          return (
            <img src={URL + img.poster} alt="" />
          )
        })}
      </div>

      <div className="row-fav">
        {
          genres && genres.animation.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.comedy.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.crime.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.documentary.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.drama.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.family.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.fantasy.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.history.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.horror.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.music.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.mystery.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.romance.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.science_fiction.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.tv_movie.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.thriller.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.war.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.western.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.news.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.reality.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.soap.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

      <div className="row-fav">
        {
          genres && genres.talk.map((img: any) => {
            return (
              <img src={URL + img.poster} alt="" />
            )
          })
        }
      </div>

    </div >